apps:
  - script: ./dist/schedules/workerStorageEventLog.js
    name: 'worker-storage-event-data'
    watch: false
    exec_mode: fork
    env:
      NODE_ENV: production
    env_production:
      NODE_ENV: production
